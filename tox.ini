[tox]
# These versions needs to by installed on the test machine.
envlist = py37, py38, py39
skipsdist = True

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
    pytest
commands =
    # echo "Now running pytest...."
    pytest
    

